SET HEADING OFF
SET FEEDBACK OFF
SET VERIFY OFF
SET PAGESIZE 0

SPOOL &1\Analizar.sql;
	SELECT 'ANALYZE table ' || table_NAME || ' COMPUTE STATISTICS;' 
	FROM user_tables
	UNION ALL
	SELECT 'ANALYZE INDEX ' || INDEX_NAME || ' COMPUTE STATISTICS;' 
	FROM USER_INDEXES;
	SELECT 'EXIT;' FROM DUAL;
SPOOL OFF;

Exit;
